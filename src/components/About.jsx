import { motion } from "framer-motion";

function About() {
  return (
<section id="about">
  <div class="card">
    <h2>About Me</h2>
    <p>
      React Developer fresher with knowledge of React.js, JavaScript, HTML, CSS, and Excel.
    </p>
  </div>
</section>


  );
}

export default About;
